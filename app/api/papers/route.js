import { NextResponse } from "next/server"; import { getSession } from "@/lib/auth"; import { getPaperTree } from "@/lib/drive"; export async function GET() { const session = await getSession(); if (!session) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (session.status !== "approved" && session.role !== "admin") return NextResponse.json({ error: "Access denied" }, { status: 403 }); try { const tree = await getPaperTree(); return NextResponse.json({ tree }) } catch (err) { console.error(err); return NextResponse.json({ error: "Failed to fetch papers" }, { status: 500 }) } }
