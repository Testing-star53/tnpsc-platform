import { NextResponse } from "next/server"; import { getSession } from "@/lib/auth"; import { supabaseAdmin } from "@/lib/supabase"; export async function PATCH(request) { const session = await getSession(); if (!session) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (session.role !== "admin") return NextResponse.json({ error: "Forbidden" }, { status: 403 }); const { userId, status } = await request.json(); if (!userId || !["approved","blocked","pending"].includes(status)) return NextResponse.json({ error: "Invalid request" }, { status: 400 }); if (userId === session.id) return NextResponse.json({ error: "Cannot change own status" }, { status: 400 }); const { error } = await supabaseAdmin.from("users").update({ status }).eq("id", userId); if (error) return NextResponse.json({ error: "Update failed" }, { status: 500 }); return NextResponse.json({ success: true }) }
