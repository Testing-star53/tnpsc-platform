import { NextResponse } from "next/server"; import { verifyToken } from "./lib/auth"; export async function middleware(request) { const { pathname } = request.nextUrl; if (pathname.startsWith("/dashboard")) { const token = request.cookies.get("tnpsc_session")?.value; if (!token) return NextResponse.redirect(new URL("/login", request.url)); const user = await verifyToken(token); if (!user) return NextResponse.redirect(new URL("/login", request.url)); if (user.status !== "approved") return NextResponse.redirect(new URL("/pending", request.url)); } if (pathname.startsWith("/admin")) { const token = request.cookies.get("tnpsc_session")?.value; if (!token) return NextResponse.redirect(new URL("/login", request.url)); const user = await verifyToken(token); if (!user || user.role !== "admin") return NextResponse.redirect(new URL("/dashboard", request.url)); } return NextResponse.next(); } export const config = { matcher: ["/dashboard/:path*", "/admin/:path*", "/login", "/register"] }
